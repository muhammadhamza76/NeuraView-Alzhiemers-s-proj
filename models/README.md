Trained model weights (best_model.pt, best_model_augmented.pt) are not
committed here directly. Generate them by running scripts/stage3_train_baseline.py
and scripts/stage6c_train_augmented.py, or download them from [add your
hosted link here, e.g. a Kaggle output or Drive link] if you'd rather not retrain.
